import { UserHasRolesDirective } from './user-has-roles.directive';

describe('UserHasRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new UserHasRolesDirective();
    expect(directive).toBeTruthy();
  });
});
